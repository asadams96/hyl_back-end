# Nom de l'application
spring.application.name=config-server

# Port de déploiement
server.port=${server.port ? server.port : 9090}

# Depot git où aller chercher les configs des autres modules
spring.cloud.config.server.git.uri=${spring.cloud.config.server.git.uri ? spring.cloud.config.server.git.uri :https://github.com/asadams96/hyl-config-repo.git}

# End points exposé par spring boot acturator
management.endpoints.web.exposure.include=*

# Informations a faire apparaitre dans l'end point "info"
info.app.version=1.0
info.app.dev=De Abreu Miranda Ayrton
